#include "memorytool.hpp"


namespace GM {
  void *MemoryTool::alloc(size_t s, char *when = NULL) {
    if (when != NULL)
      printf("memory alloc : %d bytes at %s\n", (int)s, when);
    return malloc(s);
  }
}
